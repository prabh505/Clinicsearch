# ClinicSearch - Pinned dependencies for macOS Apple Silicon (M2)
# All versions verified compatible with Python 3.12.x on arm64
# Last verified: May 2026

# ---- Ollama client ----
ollama==0.6.0

# ---- Vector database ----
chromadb==1.5.9

# ---- Web UI ----
streamlit==1.57.0

# ---- PDF parsing ----
pymupdf==1.27.2.3

# ---- Voice transcription (faster-whisper - NOT openai-whisper) ----
# faster-whisper is 4x faster than openai-whisper and avoids PyTorch on Apple Silicon
faster-whisper==1.2.0
ctranslate2==4.6.0

# ---- Audio capture ----
sounddevice==0.5.5
soundfile>=0.12.1
numpy>=1.26,<2.3

# ---- Utilities ----
python-dotenv>=1.0
tqdm>=4.66
requests>=2.31

# ---- HTTP client (for OpenFDA download) ----
httpx>=0.27

# ---- Pydantic (used internally by chromadb and langchain) ----
pydantic>=2.7,<3.0
